To install the required dependencies, run the following command in your project directory:

\`\`\`bash
npm install axios react-hook-form @hookform/resolvers yup lucide-react next-themes
\`\`\`

This will install:
- axios: For API calls
- react-hook-form & yup: For form validation
- lucide-react: For icons
- next-themes: For theme support
\`\`\`

Now, let's create the main layout for our application:
